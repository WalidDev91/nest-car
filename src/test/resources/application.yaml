spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fleetdb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none

  sql:
    init:
      mode: never

  mail:
    host: localhost
    port: 25
    username: test@example.com
    password: test-password

JWT_SECRET: dummytestsecretkeythatisatleast32byteslong12345
APP_BASE_URL: http://localhost:8080

textbee:
  api-key: dummy_textbee_api_key
  device-id: dummy_device_id

app:
  upload:
    dir: uploads
    base-url: http://localhost:8080